Placeholder cover art for a course or köşk — a soft hue wash with an arabesque arc pattern, used wherever a real cover image would go.

```jsx
<CoverPattern hue={145} height={132} label="Sarf" dense />
```

Pick `hue` per subject so a course keeps the same color everywhere it appears. Keep the
same hue for a given course across list card, hero, and preview.
