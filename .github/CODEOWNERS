# Code owners (MDRS-49, MDRS-54, MDRS-59).
#
# ADVISORY: ruleset 20827887 does not set `require_code_owner_review`, so any
# single approval satisfies it. This file only auto-requests reviewers.
#
# The team's code review assignment rotates a subset — round-robin, 2 reviewers,
# notify entire team off — and applies to the catch-all only. Rotation is a team
# feature: named individuals are BOTH requested, every time.
# https://github.com/orgs/amel-tech/teams/medaris/settings
#
# GitHub applies the LAST matching rule, so order is specific-after-general.

# Everything.
* @amel-tech/medaris

# The paths that can disable the other gates — a change to .github/ can drop a
# required check, a change to tools/ci/ can make a gate pass vacuously. Named
# rather than rotated, so the same two people see every change to them.
/.github/ @EsTharian @safakadir
/tools/ci/ @EsTharian @safakadir
